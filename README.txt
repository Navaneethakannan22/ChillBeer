# To run all Test run below cmd
./gradlew clean runMyTests

# To run 'SMOKE' Test run below cmd
./gradlew clean runMyTests -Dtags=SMOKE

# To run 'REGRESSION' Test run below cmd
./gradlew clean runMyTests -Dtags=REGRESSION